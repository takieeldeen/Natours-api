header.header
    nav.nav.nav--tours
        a.nav__el(href='#') All tours 
    .header__logo: img(src='/img/logo-white.png' alt='Natours Logo')
    nav.nav.nav--user   
        if user
            a.nav__el.nav__el--logout Logout
            a.nav__el(href='#') My bookings 
            a.nav__el(href='/me')
                img.nav__user-img(alt='user photo' src=`/img/users/${user.photo}`)
                span= user.name
        else
            a.nav__el(href='/login') Log in 
            a.nav__el.nav__el--cta Sign Up